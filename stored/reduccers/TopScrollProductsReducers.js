import TOPSCROLLPRODUTS from "../../data/TopScrollProductData";
const initialState = {
  TopScrollProducts: TOPSCROLLPRODUTS,
};

export default (state = initialState, action) => {
  return state;
};
